[package]
name = "livtet-temporal-quotes"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[package.metadata.workspaces]
independent = true

[dependencies]
blake3 = "1.8"
time = { version = "0.3", features = ["local-offset"] }
